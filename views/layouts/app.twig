<!DOCTYPE html>
<html>
    <head>
        {% block head %}
                <style> 
                body {
                    margin: 0px;
                }
                #easy {
                    display: flex;
                    justify-content: center;
                    color: white;
                    padding: 0.1rem;
                    background-color: lightgreen;
                }

                #medium {
                    display: flex;
                    justify-content: center;
                    color: white;              
                    padding: 0.1rem;
                    background-color: orange;
                }

                #hard {
                    display: flex;
                    justify-content: center;
                    color: white;
                    padding: 0.1rem;
                    background-color: red;
                }

                .navbar {
                    background-color: #ADD8E6;
                }
                </style>
            <title>My Fullstack Framework</title>
            <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}
    </head>
    <body>
  <!-- Navbar -->
  {% block body %}
    <header>
        <nav class="navbar navbar-expand-lg shadow-md py-2 mb-5 relative flex   items-center w-full justify-between">
            <div class="px-6 w-full flex flex-wrap items-center justify-center">
                <div class="navbar-collapse collapse grow items-center">
                    <ul class="navbar-nav mr-auto flex flex-row">
                        <li class="nav-item">
                            <a class="nav-link block pr-2 lg:px-2 py-2 text-gray-600 hover:text-gray-700 focus:text-gray-700 transition duration-150 ease-in-out" href="/kitchen" data-mdb-ripple="true" data-mdb-ripple-color="light">Kitchen</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link block pr-2 lg:px-2 py-2 text-gray-600 hover:text-gray-700 focus:text-gray-700 transition duration-150 ease-in-out" href="/recipe" data-mdb-ripple="true" data-mdb-ripple-color="light">Recipes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link block pr-2 lg:px-2 py-2 text-gray-600 hover:text-gray-700 focus:text-gray-700 transition duration-150 ease-in-out"
                            {% if user is defined %} href="/user/login" 
                            {% else %} href="/user/login" {% endif %}
                            href="/user/login" 
                            data-mdb-ripple="true"
                            data-mdb-ripple-color="light">{% if user is defined %}Log out -> {{ user.username }} {% else %}Log in{% endif %}</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    {% endblock %}
    </body>
</html>